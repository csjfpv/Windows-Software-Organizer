# 贡献指南

1. 从最新的 `main` 创建功能或修复分支。
2. 保持改动范围清晰，避免混入本地配置和构建产物。
3. 行为发生变化时添加或更新测试。
4. 运行 `pnpm lint`、`pnpm test` 和 `pnpm build`。
5. Pull Request 说明改动、原因、验证、风险和未验证事项。

禁止提交软件安装包、固件、设备读回、日志、用户配置、令牌、私钥、证书或客户数据。
